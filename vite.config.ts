import { defineConfig } from 'vite'

export default defineConfig({
  // Relative base so the build works at any URL — github.io/<repo>/, a
  // custom domain, or file previews — without configuration.
  base: './',
})
